import { AnyMap } from "@lib/common-defs"

export const hostSettings = {
  domains: {
    MAIN: 'dev.metastudio-news-front.ru',
  },
  environment: {
    name: 'production', // pass 'development' if you want turn on redux tools on production server
  },
  https: {
    port: 8223,
    certFileName: path.resolve(__dirname, './ssl/metastudio.crt'),
    keyFileName: path.resolve(__dirname, './ssl/metastudio.key'),
  },
  http: {
    port: 3001,
  },
  protocol: 'https:',
}

export const apiSettings = {
  prefix: (currentDomain?: string) => {
    const apiRu = 'https://dev.metastudio-news-api.ru'
    const apis: AnyMap = {}
    const { domains } = hostSettings
    apis[domains.MAIN] = apiRu
    return apis[currentDomain]
  }
}

type Locale = {
  RU?: string;
  EN?: string;
}

export const availableLocales = (currentDomain?: string): Array<Locale> => {
  return [
    { RU: "ru" },
    { EN: "en" },
  ]
}

export const defaultThemeName = 'Bootstrap'

export const applicationSecret = 'vliwerubvgiwerutbgowierutgwoeriutghweroigweboi'
